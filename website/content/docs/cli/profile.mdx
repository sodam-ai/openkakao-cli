---
title: me / profile / settings
description: Profile and account information.
---

## me

Show your profile.

```bash
openkakao-cli me [--json]
```

`me` prefers the REST account profile path, but if that fails it now falls back to the local LOCO friend graph built from known chats.

---

## profile

Show a friend's profile.

```bash
openkakao-cli profile <user_id> [--json]
openkakao-cli profile <user_id> --chat-id <chat_id>
openkakao-cli profile <user_id> --local
```

Behavior:

- plain `profile <user_id>` tries REST first, then falls back to the local LOCO graph if REST is unhealthy
- `--chat-id` uses chat-scoped LOCO `GETMEM`
- `--local` skips straight to the local LOCO graph

---

## profiles

List multi-profiles.

```bash
openkakao-cli profiles [--json]
```

---

## settings

Show account settings.

```bash
openkakao-cli settings [--json]
```

---

## keywords

Show notification keywords.

```bash
openkakao-cli keywords [--json]
```
