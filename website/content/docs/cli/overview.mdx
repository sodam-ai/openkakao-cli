---
title: CLI Overview
description: Global options and command structure.
---

import { BellRing, MessageSquareText, Settings2, ShieldCheck } from 'lucide-react';

## Introduction

`openkakao-cli` is organized around a small set of transport and automation surfaces rather than one giant command tree you need to memorize at once.

The practical split is simple:

<Cards>
  <Card icon={<MessageSquareText />} title="REST reads">
    Lightweight, cache-backed reads and account checks when you want cheap, simple commands.
  </Card>
  <Card icon={<BellRing />} title="LOCO workflows">
    Real chat operations such as full reads, sending, watch mode, and media flows.
  </Card>
  <Card icon={<ShieldCheck />} title="Policy flags">
    Unattended mode and `allow-*` flags keep risky behavior explicit instead of hidden.
  </Card>
  <Card icon={<Settings2 />} title="Persistent config">
    Repeated operator policy can live in `config.toml` while concrete targets stay on the CLI.
  </Card>
</Cards>

<Callout title="Learn the boundary, not just the names">
  The fastest way to navigate the CLI is to understand when you want a cheap REST read, when you need LOCO, and when automation policy should stay explicit.
</Callout>

## Usage

```bash
openkakao-cli [OPTIONS] <COMMAND>
```

## Global Options

| Flag | Description |
|------|-------------|
| `--json` | Output as JSON |
| `--no-color` | Disable colored output |
| `--unattended` | Declare non-interactive operation |
| `--allow-non-interactive-send` | Allow `send -y` and file send `-y` in unattended mode |
| `--allow-watch-side-effects` | Allow watch read receipts, hooks, and webhooks in unattended mode |
| `--no-prefix` | Don't prepend `🤖 [Sent via openkakao]` to outgoing messages |
| `--completion-promise` | Print `[DONE]` to stdout after successful command completion |
| `--version` | Show version |
| `--help` | Show help |

The `NO_COLOR` environment variable is also respected per [no-color.org](https://no-color.org/).

Persistent policy can be stored in [Configuration](/docs/getting-started/configuration). Transport choice guidance lives in [REST vs LOCO](/docs/getting-started/transport-boundary).

## High-Level Command Groups

<Cards>
  <Card title="Authentication" href="/docs/getting-started/authentication">
    Login extraction, relogin, renewal, and token validation.
  </Card>
  <Card title="Message reads" href="/docs/cli/message">
    `read`, `read --rest`, `search`, and `export` for message history and retrieval.
  </Card>
  <Card title="Real-time watch" href="/docs/cli/watch">
    Event-driven monitoring with reconnect handling, hooks, and webhooks.
  </Card>
  <Card title="Operator tooling" href="/docs/security/trust-model">
    Diagnostics, policy flags, and trust boundary considerations around live automation.
  </Card>
</Cards>

## Commands

### Authentication
| Command | Description |
|---------|-------------|
| `auth` | Verify token validity |
| `login --save` | Extract credentials from KakaoTalk |
| `relogin` | Refresh token via login.json |
| `renew` | Renew token via refresh_token |

### Local Database (Safe)
| Command | Description |
|---------|-------------|
| `local-chats` | List chats from local KakaoTalk DB (no server contact) |
| `local-read <chat_id>` | Read messages from local DB |
| `local-search <query>` | Search messages in local DB |
| `local-schema` | Show local DB schema |

### Chat & Messages
| Command | Description |
|---------|-------------|
| `chats` | List chat rooms |
| `chats --rest` | Force cache-backed REST chat list |
| `read <chat_id>` | Read messages (LOCO by default) |
| `read <chat_id> --rest` | Force cache-backed REST read |
| `members <chat_id>` | List members (LOCO by default) |
| `members <chat_id> --rest` | Force REST member list |
| `search <chat_id> <query>` | Search messages |
| `export <chat_id>` | Export messages (JSON/CSV/TXT) |
| `unread` | List unread chats |
| `loco-chats` | Hidden legacy compatibility command |
| `loco-read <chat_id>` | Legacy compatibility command |
| `chatinfo <chat_id>` | Chat room details (LOCO) |
| `loco-members <chat_id>` | Hidden legacy compatibility command |
| `loco-chatinfo <chat_id>` | Hidden legacy compatibility command |
| `send <chat_id> <msg>` | Send text message (requires `allow_loco_write`, supports `--dry-run`) |
| `send --me <msg>` | Send to memo chat (나와의 채팅) |
| `send-file <chat_id> <file>` | Send file (photo/video/doc) (supports `--dry-run`) |
| `send-photo <chat_id> <file>` | Send photo (alias for send-file) (supports `--dry-run`) |
| `edit <chat_id> <log_id> <msg>` | Edit a message via REWRITE (macOS: -203) (supports `--dry-run`) |
| `delete <chat_id> <log_id>` | Delete a message via DELETEMSG (supports `--dry-run`) |
| `mark-read <chat_id> <log_id>` | Mark messages as read up to logId |
| `react <chat_id> <log_id>` | Add reaction to a message (type=1 = like) (supports `--dry-run`) |
| `watch` | Watch real-time messages |
| `download <chat_id> <log_id>` | Download media attachment |
| `loco-blocked` | Inspect LOCO block/hidden-style members |

### Friends & Profile
| Command | Description |
|---------|-------------|
| `friends` | List friends |
| `profile <user_id>` | Friend profile |
| `me` | Your profile |
| `settings` | Account settings |
| `profiles` | Multi-profile list |

### Utilities
| Command | Description |
|---------|-------------|
| `doctor` | Diagnostics |
| `doctor --loco` | Test LOCO booking and connectivity |
| `loco-test` | Hidden legacy compatibility command |
| `probe <method>` | Raw LOCO probe |
| `loco-probe <method>` | Hidden legacy compatibility command |
| `scrap <url>` | Link preview (OG tags) |
| `completions <shell>` | Shell completions |

## Where to Start

<Cards>
  <Card title="Getting Started" href="/docs/getting-started/quickstart">
    Install, authenticate, and read your first chat.
  </Card>
  <Card title="read" href="/docs/cli/message">
    Choose the right message path before you build exports or automation.
  </Card>
  <Card title="watch" href="/docs/cli/watch">
    Move into event-driven workflows once the read path is already solid.
  </Card>
  <Card title="Configuration" href="/docs/getting-started/configuration">
    Persist unattended and recovery policy for long-running jobs.
  </Card>
</Cards>
