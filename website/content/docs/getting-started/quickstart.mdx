---
title: Getting Started
description: The shortest path from install to a real chat read.
icon: Rocket
---

import { KeyRound, ListTree, MessageSquareText, Terminal } from 'lucide-react';

<Callout type="warn" title="Deprecated — authentication is broken (2026-06)">
This project is no longer actively maintained, and login no longer works on recent KakaoTalk macOS builds ([#15](https://github.com/JungHoonGhae/openkakao-cli/issues/15), [#20](https://github.com/JungHoonGhae/openkakao-cli/issues/20), [#22](https://github.com/JungHoonGhae/openkakao-cli/issues/22)). The "Authenticate once" step below will fail. **Do not repeatedly retry login — it can get your account's sub-device login blocked.** Only the read-only `local-*` commands still work.
</Callout>

## Introduction

This path is intentionally narrow: install, authenticate, inspect a chat, then stop and evaluate whether the trust model fits your use case.

<Cards>
  <Card icon={<Terminal />} title="Install the CLI">
    Start with a local binary and keep the workflow on your own machine.
  </Card>
  <Card icon={<KeyRound />} title="Authenticate once">
    Reuse a saved local login before you build any higher-level automation.
  </Card>
  <Card icon={<ListTree />} title="Inspect chats first">
    List chats and read a small slice before you design summaries, exports, or hooks.
  </Card>
  <Card icon={<MessageSquareText />} title="Send only if needed">
    Outbound actions should come last, after you understand the trust and transport boundary.
  </Card>
</Cards>

<Callout title="Before you wire automation">
  If you are deciding between account checks and real chat access, read [REST vs LOCO](/docs/getting-started/transport-boundary) before building around watch, send, or media flows.
</Callout>

<Callout type="warn" title="Account risk is real">
  Depending on how you use OpenKakao, Kakao may interpret that behavior as a violation of its Terms of Service or operating policies. That can lead to account restrictions, suspension, or permanent deletion.
</Callout>

## Automatic Installation

```bash
brew tap JungHoonGhae/openkakao
brew install openkakao-cli
```

## Authenticate

Make sure KakaoTalk is running and already logged in.

```bash
openkakao-cli login --save
openkakao-cli auth
```

## Inspect a Real Chat

List chats first:

```bash
openkakao-cli chats
```

Then read only a small slice:

```bash
openkakao-cli read <chat_id> -n 20
```

## Export Before You Automate

Move one read into a structured format before you connect other tools:

```bash
openkakao-cli read <chat_id> -n 20 --json | jq '.'
```

## Send Only If You Need It

```bash
openkakao-cli send <chat_id> "Hello from CLI!"
```

<Callout title="Default prefix stays on for a reason">
  By default, sent messages include a visible prefix. Keep that behavior unless you have a strong reason not to and you fully understand the operational risk.
</Callout>

## Next

<Cards>
  <Card title="Authentication" href="/docs/getting-started/authentication">
    Understand relogin, renewal, and unattended auth policy.
  </Card>
  <Card title="REST vs LOCO" href="/docs/getting-started/transport-boundary">
    Decide which transport belongs to which task.
  </Card>
  <Card title="Troubleshooting" href="/docs/getting-started/troubleshooting">
    Resolve common failures before they become automation problems.
  </Card>
  <Card title="Safe Usage" href="/docs/security/safe-usage">
    Review the operating boundary before enabling hooks or sends.
  </Card>
</Cards>
